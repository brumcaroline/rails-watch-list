<div class="container d-flex justify-content-between align-items-center">
<h1>Fav Movies (:</h1>
<%= link_to 'Create New List', new_list_path, class: "btn btn-outline-dark" %>
</div>

<% @lists.each do |list| %>
  <div class="container my-4">
      <h2><%= link_to  list.name, list_path(list) %></h2>
  </div>
<% end %>
